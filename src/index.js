import NotableTour from './NotableTour.js';
///const NotableTour = require('./NotableTour');

function start(config = {}) {
    NotableTour.start(config);
}

export default start;
// module.exports.Start = start;