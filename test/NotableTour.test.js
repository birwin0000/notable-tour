import { expect } from 'chai';
// import { NotableTour } from '../src/NotableTour.js';
import pkg from 'jsdom';
const { JSDOM } = pkg;

describe('Main Component Tests', () => {
    it('Test Tour is object', () => {
        //let nt = new NotableTour();
        //expect(nt).to.be.an.instanceOf(NotableTour);
    });
});
